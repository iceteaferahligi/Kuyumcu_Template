@{
    var grid = Html.DevExpress().GridView(settings =>
    {
        settings.Name = "StokListesiPartial";
        settings.CallbackRouteValues = new { Controller = "Kuyumcu", Action = "StokListesiPartial" };


        ////settings.SettingsEditing.AddNewRowRouteValues = new { Controller = "Finans", Action = "BEMGridViewPartialAddNew" };
        ////settings.SettingsEditing.UpdateRowRouteValues = new { Controller = "Finans", Action = "BEMGridViewPartialUpdate" };
        //settings.SettingsEditing.DeleteRowRouteValues = new { Controller = "Finans", Action = "BorcEmanetDetayPartialDelete" };
        ////settings.SettingsEditing.Mode = GridViewEditingMode.EditFormAndDisplayRow;
        //settings.SettingsBehavior.ConfirmDelete = true;

        settings.CommandColumn.Visible = true;
        //      settings.CommandColumn.ShowNewButton = true;
        //settings.CommandColumn.ShowDeleteButton = true;
        //settings.SettingsCommandButton.DeleteButton.Text = "Sil";
        //      settings.CommandColumn.ShowEditButton = true;

        settings.KeyFieldName = "id";
        settings.SettingsPager.PageSize = 20;
        settings.SettingsPager.Visible = true;
        settings.Settings.ShowGroupPanel = true;
        settings.Settings.ShowFilterRow = true;
        settings.SettingsBehavior.AllowSelectByRowClick = true;
        //settings.ClientSideEvents.RowClick = "OnClientClick";


        settings.SettingsAdaptivity.AdaptivityMode = GridViewAdaptivityMode.Off;
        settings.SettingsAdaptivity.AdaptiveColumnPosition = GridViewAdaptiveColumnPosition.Right;
        settings.SettingsAdaptivity.AdaptiveDetailColumnCount = 1;
        settings.SettingsAdaptivity.AllowOnlyOneAdaptiveDetailExpanded = false;
        settings.SettingsAdaptivity.HideDataCellsAtWindowInnerWidth = 0;



        //int count = 1;

        //foreach (var item in Model)
        //{
        //    item.id = count;
        //    count++;
        //}
        //settings.Columns.Add("id");
         settings.Columns.Add(column =>
        {
            column.FieldName = "Tarih";
            column.ColumnType = MVCxGridViewColumnType.DateEdit;
            var colSettings = column.PropertiesEdit as DateEditProperties;
            colSettings.DisplayFormatString = "dd/MM/yyyy";
            colSettings.DisplayFormatInEditMode = true;
            column.SortOrder = DevExpress.Data.ColumnSortOrder.Descending;
        });

        settings.Columns.Add("StokId","Stok Kodu");
        settings.Columns.Add("StokCinsi");
        settings.Columns.Add("StokGrubu");
        settings.Columns.Add("StokAltGrubu");
        settings.Columns.Add("Miktar");
        settings.Columns.Add("SatisTutari","Satış Tutarı");
        settings.Columns.Add("EtiketFiyat","Etiket Fiyatı");
        settings.Columns.Add("Maliyet");
        settings.Columns.Add("BirimFiyat");
        settings.Columns.Add("ToptanciAdi","Toptancı Adı");
        settings.Columns.Add("Ozellik1","Özellik 1");
        settings.Columns.Add("Ozellik2","Özellik 2");
        settings.Columns.Add("Ozellik4","Özellik 4");
       

        //settings.Init = (s, e) =>
        //{


        //    var GridFaturaPartial = (s as MVCxGridView);

        //    for (int i = 0; i < GridFaturaPartial.Columns.Count; i++)
        //    {
        //        var column = GridFaturaPartial.Columns[i] as GridViewDataColumn;
        //        if (column != null)
        //        {
        //            column.FieldName["Tarih"].SortOrder = DevExpress.Data.ColumnSortOrder.Descending;
        //            column.SortIndex = i;
        //        }
        //    }
        //};



    });
    if (ViewData["EditError"] != null)
    {
        grid.SetEditErrorText((string)ViewData["EditError"]);
    }
}

@grid.Bind(Model).GetHtml()

